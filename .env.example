# SM Arena environment
BOT_TOKEN=

# Admins: comma-separated telegram user IDs (for /stats, /broadcast, etc.)
ADMIN_IDS=

# LiqPay keys (use sandbox while testing)
LIQPAY_PUBLIC_KEY=
LIQPAY_PRIVATE_KEY=

# Public base URL of webhook server (HTTPS). For local tests use ngrok URL.
WEBHOOK_BASE_URL=

# Webhook server bind (leave defaults for local)
WEBHOOK_HOST=0.0.0.0
WEBHOOK_PORT=8080

# Optional database path (use /data/sm_arena.db with Railway Volume)
DB_PATH=
